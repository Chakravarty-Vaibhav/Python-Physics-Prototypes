import numpy as np
import matplotlib.pyplot as plt


t = np.linspace(0, 4*np.pi, 300)
x = np.cos(t)
y = np.sin(t)
z = t / (4*np.pi)

fig = plt.figure()
ax = fig.add_subplot(111, projection='3d')
ax.plot(x, y, z)
ax.set_title("Helical path in 3D")
plt.show()
